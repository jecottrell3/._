;;;;(setq Buffer-menu-buffer-column)
;;;;(setq Buffer-menu-mode-map)
;;;;(setq Buffer-menu-size-column)
;;;;(setq Info-current-file)
;;;;(setq Info-current-file-completions)
;;;;(setq Info-current-node)
;;;;(setq Info-current-subfile)
;;;;(setq Info-default-directory-list)
;;;;(setq Info-dir-contents)
;;;;(setq Info-dir-contents-directory)
;;;;(setq Info-dir-file-attributes)
;;;;(setq Info-directory-list)
;;;;(setq Info-edit-map)
;;;;(setq Info-enable-active-nodes)
;;;;(setq Info-enable-edit)
;;;;(setq Info-fontify)
;;;;(setq Info-history)
;;;;(setq Info-index-alternatives)
;;;;(setq Info-last-search)
(setq Info-mode-hook 'Info-mode-hook)
;;;;(setq Info-mode-map)
;;;;(setq Info-selection-hook)
;;;;(setq Info-standalone)
;;;;(setq Info-suffix-list)
;;;;(setq Info-tag-table-marker)
;;;;(setq Man-auto-section-alist)
;;;;(setq Man-circular-pages-p)
;;;;(setq Man-current-page)
;;;;(setq Man-downcase-section-letters-p)
;;;;(setq Man-filter-list)
;;;;(setq Man-first-heading-regexp)
;;;;(setq Man-frame-parameters)
;;;;(setq Man-heading-regexp)
;;;;(setq Man-mode-hooks)
;;;;(setq Man-mode-line-format)
;;;;(setq Man-mode-map)
;;;;(setq Man-notify)
;;;;(setq Man-page-list)
;;;;(setq Man-page-mode-string)
;;;;(setq Man-reference-regexp)
;;;;(setq Man-refpages-alist)
;;;;(setq Man-reuse-okay-p)
;;;;(setq Man-section-regexp)
;;;;(setq Man-section-translations-alist)
;;;;(setq Man-sections-alist)
;;;;(setq Man-see-also-regexp)
;;;;(setq Man-specified-section-option)
;;;;(setq Man-switches)
;;;;(setq abbrev-all-caps)
;;;;(setq abbrev-file-name)
;;;;(setq abbrev-mode)
;;;;(setq abbrev-start-location)
;;;;(setq abbrev-start-location-buffer)
;;;;(setq abbrev-table-name-list)
;;;;(setq abbreviated-home-dir)
;;;;(setq abbrevs-changed)
;;;;(setq ad-activate-on-definition)
;;;;(setq ad-definition-hooks)
;;;;(setq ad-enable-definition-hooks)
;;;;(setq ad-redefinition-action)
;;;;(setq ad-start-advice-on-load)
;;;;(setq ad-use-jwz-byte-compiler)
;;;;(setq adaptive-fill-mode)
;;;;(setq adaptive-fill-regexp)
;;;;(setq after-change-function)
;;;;(setq after-init-hook)
;;;;(setq after-insert-file-functions)
;;;;(setq after-load-alist)
;;;;(setq appt-audible)
;;;;(setq appt-display-duration)
;;;;(setq appt-display-mode-line)
;;;;(setq appt-issue-message)
;;;;(setq appt-message-warning-time)
;;;;(setq appt-msg-window)
;;;;(setq appt-visible)
;;;;(setq auto-fill-function)
;;;;(setq auto-fill-inhibit-regexp)
(defvar orig-mode-alist auto-mode-alist
  "Original value of auto-mode-alist")
(setq also-mode-alist
      '(("^[Mm]akefile$" . makefile-mode)
	("\\.cpp$" . c++-mode)
	("\\.h$" . c-mode)
	("\\.n$" . nroff-mode)
	("\\.p[hl]$" . perl-mode)))
(setq auto-mode-alist (append also-mode-alist orig-mode-alist))
;;;;(setq auto-save-default)
;;;;(setq auto-save-interval)
;;;;(setq auto-save-timeout)
;;;;(setq auto-save-visited-file-name)
;;;;(setq automount-dir-prefix)
;;;;(setq backquote-splice)
;;;;(setq backquote-unquote)
(setq backup-by-copying nil)
(setq backup-by-copying-when-linked t)
(setq backup-by-copying-when-mismatch nil)
;;;;(setq backup-enable-predicate)
;;;;(setq backup-inhibited)
;;;;(setq baud-rate)
;;;;(setq before-change-function)
;;;;(setq before-init-hook)
;;;;(setq blink-matching-paren)
;;;;(setq blink-matching-paren-distance)
(setq blink-paren-function 'blink-paren-hook)
;;;;(setq bookmark-map)
;;;;(setq bq-evalfns)
;;;;(setq bq-quotefns)
;;;;(setq bq-singles)
;;;;(setq bq-splicefns)
;;;;(setq buffer-auto-save-file-name)
;;;;(setq buffer-backed-up)
;;;;(setq buffer-display-table)
;;;;(setq buffer-file-name)
;;;;(setq buffer-file-number)
;;;;(setq buffer-file-truename)
(setq buffer-menu-mode-hook 'buffer-menu-mode-hook)
;;;;(setq buffer-offer-save)
;;;;(setq buffer-read-only)
;;;;(setq buffer-saved-size)
;;;;(setq buffer-undo-list)
;;;;(setq buffers-menu-max-size)
(setq c-argdecl-indent 4)
(setq c-auto-newline nil)
(setq c-backslash-column 48)
(setq c-brace-imaginary-offset 0)
(setq c-brace-offset -4)
(setq c-continued-brace-offset 0)
(setq c-continued-statement-offset 4)
(setq c-indent-level 4)
(setq c-label-offset -4)
;;;;(setq c-mode-abbrev-table)
(setq c-mode-hook 'c-mode-hook)
(setq c++-mode-hook 'c++-mode-hook)
;;;;(setq c-mode-map)
;;;;(setq c-mode-syntax-table)
;;;;(setq c-style-alist)
;;;;(setq c-switch-label-regexp)
(setq c-tab-always-indent nil)
;;;;(setq case-fold-search)
;;;;(setq case-replace)
;;;;(setq cd-path)
;;;;(setq comint-after-partial-filename-command)
;;;;(setq comint-completion-addsuffix)
;;;;(setq comint-completion-autolist)
;;;;(setq comint-completion-recexact)
;;;;(setq comint-delimiter-argument-list)
;;;;(setq comint-dynamic-complete-command-command)
;;;;(setq comint-dynamic-complete-filename-command)
;;;;(setq comint-eol-on-send)
;;;;(setq comint-exec-hook)
;;;;(setq comint-file-name-prefix)
;;;;(setq comint-get-current-command)
;;;;(setq comint-get-old-input)
;;;;(setq comint-input-autoexpand)
;;;;(setq comint-input-chunk-size)
;;;;(setq comint-input-filter)
;;;;(setq comint-input-ignoredups)
;;;;(setq comint-input-ring)
;;;;(setq comint-input-ring-file-name)
;;;;(setq comint-input-ring-index)
;;;;(setq comint-input-ring-size)
;;;;(setq comint-input-sender)
;;;;(setq comint-input-sentinel)
;;;;(setq comint-matching-input-from-input-string)
(setq comint-mode-hook 'comint-mode-hook)
;;;;(setq comint-mode-map)
;;;;(setq comint-output-filter-functions)
;;;;(setq comint-process-echoes)
;;;;(setq comint-prompt-regexp)
;;;;(setq comint-ptyp)
;;;;(setq comint-scroll-show-maximum-output)
;;;;(setq comint-scroll-to-bottom-on-input)
;;;;(setq comint-scroll-to-bottom-on-output)
;;;;(setq command-debug-status)
;;;;(setq command-history)
;;;;(setq command-history-hook)
;;;;(setq command-history-map)
;;;;(setq command-line-args)
;;;;(setq command-line-functions)
;;;;(setq command-line-processed)
;;;;(setq command-switch-alist)
(setq comment-column 48)
;;;;(setq comment-end)
;;;;(setq comment-indent-function)
;;;;(setq comment-indent-hook)
(setq comment-multi-line t)
;;;;(setq comment-start)
;;;;(setq comment-start-skip)
;;;;(setq compare-ignore-case)
;;;;(setq compare-windows-whitespace)
;;;;(setq compilation-buffer-name-function)
;;;;(setq compilation-directory-stack)
;;;;(setq compilation-enter-directory-regexp)
;;;;(setq compilation-error-list)
;;;;(setq compilation-error-message)
;;;;(setq compilation-error-regexp-alist)
;;;;(setq compilation-finish-function)
;;;;(setq compilation-in-progress)
;;;;(setq compilation-last-buffer)
;;;;(setq compilation-leave-directory-regexp)
;;;;(setq compilation-minor-mode)
;;;;(setq compilation-minor-mode-map)
;;;;(setq compilation-mode-hook)
;;;;(setq compilation-mode-map)
;;;;(setq compilation-old-error-list)
;;;;(setq compilation-parse-errors-function)
;;;;(setq compilation-parsing-end)
;;;;(setq compilation-search-path)
;;;;(setq compilation-window-height)
;;;;(setq compile-command)
;;;;(setq compile-history)
;;;;(setq completion-auto-help)
;;;;(setq completion-ignore-case)
;;;;(setq completion-ignored-extensions)
;;;;(setq completion-list-mode-map)
;;;;(setq completion-setup-hook)
;;;;(setq complex-buffers-menu-p)
;;;;(setq configure-info-directory)
;;;;(setq ctl-arrow)
;;;;(setq ctl-x-4-map)
;;;;(setq ctl-x-5-map)
;;;;(setq ctl-x-map)
;;;;(setq current-load-list)
;;;;(setq current-prefix-arg)
;;;;(setq cursor-in-echo-area)
;;;;(setq dabbrevs-backward-only)
;;;;(setq dabbrevs-limit)
;;;;(setq data-directory)
;;;;(setq deactivate-mark)
;;;;(setq debug-end-pos)
;;;;(setq debug-on-error)
;;;;(setq debug-on-next-call)
;;;;(setq debug-on-quit)
;;;;(setq debugger)
;;;;(setq default-abbrev-mode)
;;;;(setq default-case-fold-search)
;;;;(setq default-command-history-filter-garbage)
;;;;(setq default-ctl-arrow)
;;;;(setq default-directory)
;;;;(setq default-fill-column)
;;;;(setq default-frame-alist)
;;;;(setq default-left-margin)
;;;;(setq default-major-mode)
;;;;(setq default-minibuffer-frame)
;;;;(setq default-mode-line-format)
;;;;(setq default-tab-width)
;;;;(setq default-tags-table-function)
;;;;(setq default-truncate-lines)
;;;;(setq defining-kbd-macro)
;;;;(setq defun-prompt-regexp)
;;;;(setq degrees-to-radians)
;;;;(setq delete-auto-save-files)
;;;;(setq delete-exited-processes)
;;;;(setq diff-new-file)
;;;;(setq diff-new-temp-file)
;;;;(setq diff-old-file)
;;;;(setq diff-old-temp-file)
;;;;(setq diff-regexp-alist)
;;;;(setq diff-switches)
;;;;(setq directory-abbrev-alist)
;;;;(setq dired-actual-switches)
;;;;(setq dired-after-readin-hook)
;;;;(setq dired-backup-overwrite)
;;;;(setq dired-before-readin-hook)
;;;;(setq dired-buffers)
;;;;(setq dired-chown-program)
;;;;(setq dired-copy-preserve-time)
;;;;(setq dired-del-marker)
;;;;(setq dired-deletion-confirmer)
;;;;(setq dired-directory)
;;;;(setq dired-dwim-target)
;;;;(setq dired-flagging-regexp)
;;;;(setq dired-keep-marker-copy)
;;;;(setq dired-keep-marker-hardlink)
;;;;(setq dired-keep-marker-rename)
;;;;(setq dired-keep-marker-symlink)
(setq dired-kept-versions 10)
(setq dired-listing-switches "-alF")
(setq dired-load-hook 'dired-load-hook)
;;;;(setq dired-log-buffer)
(setq dired-ls-F-marks-symlinks t)
;;;;(setq dired-ls-sorting-switches)
;;;;(setq dired-mark-postfix)
;;;;(setq dired-mark-prefix)
;;;;(setq dired-mark-separator)
;;;;(setq dired-marker-char)
(setq dired-mode-hook 'dired-mode-hook)
;;;;(setq dired-mode-map)
(setq dired-no-confirm '(compress copy load move uncompress))
;;;;(setq dired-query-alist)
;;;;(setq dired-re-dir)
;;;;(setq dired-re-dot)
;;;;(setq dired-re-exe)
;;;;(setq dired-re-inode-size)
;;;;(setq dired-re-mark)
;;;;(setq dired-re-maybe-mark)
;;;;(setq dired-re-sym)
;;;;(setq dired-regexp-history)
;;;;(setq dired-shrink-to-fit)
;;;;(setq dired-sort-by-date-regexp)
;;;;(setq dired-sort-by-name-regexp)
;;;;(setq dired-subdir-alist)
;;;;(setq dired-subdir-regexp)
;;;;(setq dired-trivial-filenames)
;;;;(setq disabled-command-hook)
;;;;(setq display-buffer-function)
(setq display-time-24hr-format t)
;;;;(setq display-time-day-and-date)
;;;;(setq display-time-hook)
;;;;(setq display-time-interval)
;;;;(setq display-time-mail-file)
;;;;(setq display-time-process)
;;;;(setq display-time-string)
;;;;(setq double-click-time)
;;;;(setq e)
;;;;(setq echo-keystrokes)
;;;;(setq edebug-active)
;;;;(setq edebug-all-defuns)
;;;;(setq edebug-arrow-alist)
;;;;(setq edebug-backtrace)
;;;;(setq edebug-depth)
;;;;(setq edebug-entered)
;;;;(setq edebug-epoch-running)
;;;;(setq edebug-eval-list)
;;;;(setq edebug-eval-macro-args)
;;;;(setq edebug-eval-mode-map)
;;;;(setq edebug-initial-mode)
;;;;(setq edebug-initial-mode-alist)
;;;;(setq edebug-mode)
;;;;(setq edebug-mode-map)
;;;;(setq edebug-offset-indices)
;;;;(setq edebug-previous-result)
;;;;(setq edebug-recursion-depth)
;;;;(setq edebug-save-buffer-points)
;;;;(setq edebug-save-point)
;;;;(setq edebug-save-windows)
;;;;(setq edebug-stop-before-symbols)
;;;;(setq edebug-syntax-table)
;;;;(setq edebug-trace)
;;;;(setq edebug-window-start)
;;;;(setq edit-abbrevs-map)
;;;;(setq edit-tab-stops-buffer)
;;;;(setq edit-tab-stops-map)
;;;;(setq ehelp-map)
;;;;(setq electric-buffer-menu-mode-map)
;;;;(setq electric-command-history-hook)
;;;;(setq electric-help-map)
;;;;(setq electric-history-map)
;;;;(setq emacs-build-system)
;;;;(setq emacs-build-time)
;;;;(setq emacs-iconified)
;;;;(setq emacs-lisp-file-regexp)
;;;;(setq emacs-lisp-mode-map)
;;;;(setq emacs-lisp-mode-syntax-table)
;;;;(setq emacs-priority)
;;;;(setq emacs-version)
(setq enable-local-eval 'maybe)
;;;;(setq enable-local-variables)
(setq enable-recursive-minibuffers t)
;;;;(setq esc-map)
;;;;(setq exec-directory)
;;;;(setq exec-path)
;;;;(setq executing-kbd-macro)
;;;;(setq executing-macro)
;;;;(setq explicit-csh-args)
(setq explicit-shell-file-name "/bin/ksh")
;;;;(setq extended-command-history)
;;;;(setq extra-keyboard-modifiers)
;;;;(setq features)
;;;;(setq file-name-chars)
;;;;(setq file-name-handler-alist)
;;;;(setq file-name-history)
;;;;(setq file-of-tag-function)
;;;;(setq file-precious-flag)
;;;;(setq fill-column)
;;;;(setq fill-individual-varying-indent)
;;;;(setq fill-prefix)
;;;;(setq find-file-existing-other-name)
(setq find-file-hooks '(find-file-hook))
;;;;(setq find-file-not-found-hooks)
;;;;(setq find-file-not-true-dirname-list)
;;;;(setq find-file-run-dired)
;;;;(setq find-file-visit-truename)
;;;;(setq find-grep-options)
;;;;(setq find-ls-option)
;;;;(setq find-tag-default-function)
;;;;(setq find-tag-hook)
;;;;(setq find-tag-next-line-after-failure-p)
;;;;(setq find-tag-regexp-next-line-after-failure-p)
;;;;(setq find-tag-regexp-search-function)
;;;;(setq find-tag-regexp-tag-order)
;;;;(setq find-tag-search-function)
;;;;(setq find-tag-tag-order)
;;;;(setq first-change-hook)
;;;;(setq float-output-format)
;;;;(setq font-lock-mode-hook)
;;;;(setq fortran-tab-mode-default)
;;;;(setq frame-creation-function)
;;;;(setq frame-initial-frame)
;;;;(setq frame-initial-frame-alist)
;;;;(setq function-key-map)
;;;;(setq fundamental-mode-abbrev-table)
;;;;(setq gc-cons-threshold)
;;;;(setq global-abbrev-table)
;;;;(setq global-edebug-map)
;;;;(setq global-edebug-prefix)
;;;;(setq global-face-data)
;;;;(setq global-map)
;;;;(setq global-mode-string)
;;;;(setq glyph-table)
;;;;(setq goal-column)
;;;;(setq goto-tag-location-function)
;;;;(setq grep-history)
;;;;(setq grep-regexp-alist)
;;;;(setq gud-comint-buffer)
;;;;(setq gud-dbx-history)
;;;;(setq gud-delete-prompt-marker)
;;;;(setq gud-gdb-history)
;;;;(setq gud-gdb-marker-acc)
;;;;(setq gud-key-prefix)
;;;;(setq gud-last-frame)
;;;;(setq gud-last-last-frame)
(setq gud-mode-hook 'gud-mode-hook)
;;;;(setq gud-perldb-history)
;;;;(setq gud-perldb-marker-acc)
;;;;(setq gud-sdb-history)
;;;;(setq gud-sdb-lastfile)
;;;;(setq gud-sdb-needs-tags)
;;;;(setq gud-xdb-accumulation)
;;;;(setq gud-xdb-directories)
;;;;(setq gud-xdb-history);;;;(setq help-admin-map)
;;;;(setq help-apropos-map)
;;;;(setq help-char)
;;;;(setq help-form)
;;;;(setq help-keys-map)
;;;;(setq help-manual-map)
;;;;(setq help-map)
;;;;(setq help-menu-map)
;;;;(setq help-misc-map)
;;;;(setq help-modes-map)
;;;;(setq hide-ifdef-initially)
;;;;(setq hide-ifdef-lines)
;;;;(setq hide-ifdef-read-only)
;;;;(setq highlight-nonselected-windows)
;;;;(setq hilit-auto-highlight)
;;;;(setq hilit-auto-highlight-maxout)
;;;;(setq hilit-auto-rehighlight)
;;;;(setq hilit-auto-rehighlight-fallback)
;;;;(setq hilit-background-mode)
;;;;(setq hilit-default-face-table)
;;;;(setq hilit-face-check)
;;;;(setq hilit-face-translation-table)
;;;;(setq hilit-inhibit-hooks)
;;;;(setq hilit-inhibit-rebinding)
;;;;(setq hilit-mode-enable-list)
;;;;(setq hilit-parser-alist)
;;;;(setq hilit-patterns-alist)
;;;;(setq hilit-predefined-face-list)
;;;;(setq hilit-quietly)
;;;;(setq hippie-expand-max-buffers)
;;;;(setq hippie-expand-try-functions-list)
;;;;(setq hippie-expand-verbose)
;;;;(setq hscroll-step)
;;;;(setq ignored-local-variables)
;;;;(setq indent-line-function)
;;;;(setq indent-region-function)
;;;;(setq indent-tabs-mode)
;;;;(setq indented-text-mode-map)
;;;;(setq inferior-lisp-filter-regexp)
;;;;(setq inferior-lisp-load-command)
;;;;(setq inferior-lisp-mode-hook)
;;;;(setq inferior-lisp-mode-map)
;;;;(setq inferior-lisp-program)
;;;;(setq inferior-lisp-prompt)
;;;;(setq inhibit-default-init)
;;;;(setq inhibit-local-variables-regexps)
;;;;(setq inhibit-point-motion-hooks)
;;;;(setq inhibit-quit)
;;;;(setq inhibit-read-only)
(setq inhibit-startup-message t)
;;;;(setq init-file-debug)
;;;;(setq init-file-had-error)
;;;;(setq init-file-user)
;;;;(setq initial-frame-alist)
;;;;(setq initial-major-mode)
;;;;(setq insert-default-directory)
;;;;(setq insert-directory-program)
;;;;(setq internal-doc-file-name)
;;;;(setq interprogram-cut-function)
;;;;(setq interprogram-paste-function)
;;;;(setq interval-balance-threshold)
;;;;(setq inverse-video)
;;;;(setq isearch-adjusted)
;;;;(setq isearch-barrier)
;;;;(setq isearch-case-fold-search)
;;;;(setq isearch-cmds)
;;;;(setq isearch-event-data-type)
;;;;(setq isearch-forward)
;;;;(setq isearch-gnu-emacs-events)
;;;;(setq isearch-invalid-regexp)
;;;;(setq isearch-message)
;;;;(setq isearch-mode)
;;;;(setq isearch-mode-end-hook)
(setq isearch-mode-hook 'isearch-mode-hook)
;;;;(setq isearch-mode-map)
;;;;(setq isearch-new-forward)
;;;;(setq isearch-nonincremental)
;;;;(setq isearch-old-local-map)
;;;;(setq isearch-op-fun)
;;;;(setq isearch-opoint)
;;;;(setq isearch-other-end)
;;;;(setq isearch-overlay)
;;;;(setq isearch-pre-command-hook-exists)
;;;;(setq isearch-recursive-edit)
;;;;(setq isearch-regexp)
;;;;(setq isearch-slow-terminal-mode)
;;;;(setq isearch-small-window)
;;;;(setq isearch-string)
;;;;(setq isearch-success)
;;;;(setq isearch-window-configuration)
;;;;(setq isearch-word)
;;;;(setq isearch-wrapped)
;;;;(setq isearch-yank-flag)
(setq kept-new-versions 10)
(setq kept-old-versions 10)
;;;;(setq key-translation-map)
;;;;(setq keyboard-translate-table)
;;;;(setq keyboard-type)
;;;;(setq kill-emacs-hook)
;;;;(setq kill-ring)
;;;;(setq kill-ring-max)
;;;;(setq kill-ring-yank-pointer)
;;;;(setq kill-whole-line)
;;;;(setq last-abbrev)
;;;;(setq last-abbrev-location)
;;;;(setq last-abbrev-text)
;;;;(setq last-command)
;;;;(setq last-command-char)
;;;;(setq last-command-event)
;;;;(setq last-dabbrev-table)
;;;;(setq last-dabbrevs-abbrev-location)
;;;;(setq last-dabbrevs-abbreviation)
;;;;(setq last-dabbrevs-direction)
;;;;(setq last-dabbrevs-expansion)
;;;;(setq last-dabbrevs-expansion-location)
;;;;(setq last-dabbrevs-table)
;;;;(setq last-event-frame)
;;;;(setq last-input-char)
;;;;(setq last-input-end)
;;;;(setq last-input-event)
;;;;(setq last-input-start)
;;;;(setq last-kbd-macro)
;;;;(setq last-nonmenu-event)
;;;;(setq last-tag)
;;;;(setq latex-block-names)
;;;;(setq latex-run-command)
;;;;(setq ledit-go-to-lisp-string)
;;;;(setq ledit-go-to-liszt-string)
;;;;(setq ledit-save-files)
;;;;(setq left-margin)
;;;;(setq line-number-display-limit)
(setq line-number-mode t)
;;;;(setq lisp-body-indent)
;;;;(setq lisp-indent-function)
;;;;(setq lisp-indent-offset)
;;;;(setq lisp-interaction-mode-map)
;;;;(setq lisp-mode-abbrev-table)
;;;;(setq lisp-mode-map)
;;;;(setq lisp-mode-syntax-table)
;;;;(setq list-buffers-directory)
;;;;(setq list-command-history-filter)
;;;;(setq list-command-history-max)
(setq list-directory-brief-switches "-FAC")
(setq list-directory-verbose-switches "-Flast")
;;;;(setq list-faces-sample-text)
;;;;(setq list-matching-lines-default-context-lines)
;;;;(setq list-tags-function)
;;;;(setq listify-key-sequence-1)
;;;;(setq load-history)
;;;;(setq load-in-progress)
;;;;(setq load-path)
;;;;(setq local-abbrev-table)
;;;;(setq local-write-file-hooks)
;;;;(setq lpr-command)
(setq lpr-switches nil)
;;;;(setq lucid-menu-bar-dirty-flag)
;;;;(setq mail-archive-file-name)
;;;;(setq mail-header-separator)
;;;;(setq mail-interactive)
;;;;(setq mail-self-blind)
;;;;(setq mail-setup-hook)
;;;;(setq mail-signature)
;;;;(setq mail-use-rfc822)
;;;;(setq mail-yank-ignored-headers)
;;;;(setq major-mode)
;;;;(setq make-backup-files)
;;;;(setq makefile-brave-make)
;;;;(setq makefile-browser-auto-advance-after-selection-p)
;;;;(setq makefile-browser-buffer-name)
;;;;(setq makefile-browser-client)
;;;;(setq makefile-browser-cursor-column)
;;;;(setq makefile-browser-hook)
;;;;(setq makefile-browser-leftmost-column)
;;;;(setq makefile-browser-map)
;;;;(setq makefile-browser-selected-mark)
;;;;(setq makefile-browser-selection-vector)
;;;;(setq makefile-browser-unselected-mark)
;;;;(setq makefile-cleanup-continuations-p)
;;;;(setq makefile-dependency-regex)
;;;;(setq makefile-gnumake-functions-alist)
;;;;(setq makefile-has-prereqs)
;;;;(setq makefile-ignored-files-in-pickup-regex)
;;;;(setq makefile-macro-assign)
;;;;(setq makefile-macro-table)
;;;;(setq makefile-macroassign-regex)
;;;;(setq makefile-mode-hook)
;;;;(setq makefile-mode-map)
;;;;(setq makefile-mode-name)
;;;;(setq makefile-mode-syntax-table)
;;;;(setq makefile-need-macro-pickup)
;;;;(setq makefile-need-target-pickup)
;;;;(setq makefile-pickup-everything-picks-up-filenames-p)
;;;;(setq makefile-query-one-target-method)
;;;;(setq makefile-runtime-macros-list)
;;;;(setq makefile-special-targets-list)
;;;;(setq makefile-tab-after-target-colon)
;;;;(setq makefile-target-colon)
;;;;(setq makefile-target-table)
;;;;(setq makefile-up-to-date-buffer-name)
;;;;(setq makefile-use-curly-braces-for-macros-p)
;;;;(setq manual-program)
;;;;(setq mark-active)
;;;;(setq mark-even-if-inactive)
;;;;(setq mark-ring)
;;;;(setq mark-ring-max)
;;;;(setq max-lisp-eval-depth)
;;;;(setq max-specpdl-size)
;;;;(setq menu-bar-edit-menu)
;;;;(setq menu-bar-file-menu)
;;;;(setq menu-bar-final-items)
;;;;(setq menu-bar-help-menu)
;;;;(setq menu-prompt-more-char)
;;;;(setq menu-prompting)
;;;;(setq meta-prefix-char)
;;;;(setq mh-lib)
;;;;(setq mh-progs)
;;;;(setq mh-show-mode-hook)
;;;;(setq minibuffer-completion-confirm)
;;;;(setq minibuffer-completion-predicate)
;;;;(setq minibuffer-completion-table)
;;;;(setq minibuffer-frame-alist)
;;;;(setq minibuffer-help-form)
;;;;(setq minibuffer-history)
;;;;(setq minibuffer-history-position)
;;;;(setq minibuffer-history-search-history)
;;;;(setq minibuffer-history-sexp-flag)
;;;;(setq minibuffer-history-variable)
;;;;(setq minibuffer-local-completion-map)
;;;;(setq minibuffer-local-isearch-map)
;;;;(setq minibuffer-local-map)
;;;;(setq minibuffer-local-must-match-map)
;;;;(setq minibuffer-local-ns-map)
;;;;(setq minibuffer-scroll-window)
;;;;(setq minibuffer-setup-hook)
;;;;(setq minor-mode-alist)
;;;;(setq minor-mode-map-alist)
;;;;(setq mocklisp-arguments)
;;;;(setq mode-line-buffer-identification)
;;;;(setq mode-line-format)
;;;;(setq mode-line-inverse-video)
;;;;(setq mode-line-modified)
;;;;(setq mode-line-process)
;;;;(setq mode-name)
;;;;(setq mode-specific-map)
;;;;(setq mouse-avoidance-animation-delay)
(setq mouse-avoidance-mode 'cat-and-mouse)
;;;;(setq mouse-avoidance-n-pointer-shapes)
;;;;(setq mouse-avoidance-nudge-dist)
;;;;(setq mouse-avoidance-nudge-var)
;;;;(setq mouse-avoidance-pointer-shapes)
;;;;(setq mouse-avoidance-threshhold)
;;;;(setq mouse-buffer-offset)
;;;;(setq mouse-drag-overlay)
;;;;(setq mouse-grabbed)
;;;;(setq mouse-save-then-kill-posn)
;;;;(setq mouse-scroll-delay)
;;;;(setq mouse-secondary-overlay)
;;;;(setq mouse-secondary-start)
;;;;(setq mouse-sel-check-selection-function)
;;;;(setq mouse-sel-cycle-clicks)
;;;;(setq mouse-sel-default-bindings)
;;;;(setq mouse-sel-get-selection-function)
;;;;(setq mouse-sel-leave-point-near-mouse)
;;;;(setq mouse-sel-retain-highlight)
;;;;(setq mouse-sel-selection)
;;;;(setq mouse-sel-selection-type)
;;;;(setq mouse-sel-set-selection-function)
;;;;(setq mouse-sel-version)
;;;;(setq mouse-selection-click-count)
;;;;(setq news-inews-program)
;;;;(setq news-path)
;;;;(setq next-file-list)
;;;;(setq next-line-add-newlines)
;;;;(setq next-screen-context-lines)
;;;;(setq nil)
;;;;(setq nntp-buggy-select)
;;;;(setq nntp-debug-read)
;;;;(setq nntp-large-newsgroup)
;;;;(setq nntp-maximum-request)
;;;;(setq nntp-server-buffer)
;;;;(setq nntp-server-hook)
;;;;(setq nntp-server-name)
;;;;(setq nntp-server-process)
;;;;(setq nntp-status-string)
;;;;(setq nntp-version)
;;;;(setq no-redraw-on-reenter)
;;;;(setq noninteractive)
;;;;(setq num-input-keys)
;;;;(setq obarray)
;;;;(setq occur-buffer)
;;;;(setq occur-mode-map)
;;;;(setq occur-nlines)
;;;;(setq occur-pos-list)
;;;;(setq only-global-abbrevs)
;;;;(setq other-window-scroll-buffer)
;;;;(setq outline-heading-end-regexp)
;;;;(setq outline-level)
;;;;(setq outline-minor-mode)
;;;;(setq outline-minor-mode-map)
;;;;(setq outline-minor-mode-prefix)
;;;;(setq outline-mode-map)
;;;;(setq outline-regexp)
;;;;(setq overlay-arrow-position)
;;;;(setq overlay-arrow-string)
;;;;(setq overwrite-mode)
;;;;(setq overwrite-mode-binary)
;;;;(setq overwrite-mode-textual)
;;;;(setq page-delimiter)
;;;;(setq paragraph-ignore-fill-prefix)
;;;;(setq paragraph-separate)
;;;;(setq paragraph-start)
;;;;(setq parens-require-spaces)
;;;;(setq parse-sexp-ignore-comments)
;;;;(setq pending-undo-list)
;;;;(setq perl-brace-imaginary-offset)
;;;;(setq perl-brace-offset)
;;;;(setq perl-continued-brace-offset)
;;;;(setq perl-continued-statement-offset)
;;;;(setq perl-indent-level)
;;;;(setq perl-label-offset)
;;;;(setq perl-mode-abbrev-table)
;;;;(setq perl-mode-map)
;;;;(setq perl-mode-syntax-table)
;;;;(setq perl-nochange)
;;;;(setq perl-tab-always-indent)
;;;;(setq perl-tab-to-comment)
;;;;(setq perldb-mode-hook 'perldb-mode-hook)
;;;;(setq pi)
;;;;(setq polling-period)
;;;;(setq pop-up-frame-alist)
;;;;(setq pop-up-frame-function)
;;;;(setq pop-up-frames)
;;;;(setq pop-up-windows)
;;;;(setq post-command-hook)
;;;;(setq pre-abbrev-expand-hook)
;;;;(setq pre-command-hook)
;;;;(setq prefix-arg)
;;;;(setq prefix-help-command)
(setq print-escape-newlines t)
;;;;(setq print-length)
;;;;(setq print-level)
;;;;(setq print-region-function)
;;;;(setq process-connection-type)
;;;;(setq process-environment)
;;;;(setq pure-bytes-used)
;;;;(setq purify-flag)
;;;;(setq query-replace-help)
;;;;(setq query-replace-history)
;;;;(setq query-replace-map)
;;;;(setq quit-flag)
;;;;(setq radians-to-degrees)
;;;;(setq read-expression-history)
;;;;(setq read-expression-map)
;;;;(setq regexp-history)
;;;;(setq regexp-search-ring)
;;;;(setq regexp-search-ring-max)
;;;;(setq regexp-search-ring-yank-pointer)
;;;;(setq region-face)
;;;;(setq register-alist)
;;;;(setq replace-copying-with)
;;;;(setq reporter-eval-buffer)
;;;;(setq reporter-mailer)
(setq require-final-newline 1)
;;;;(setq revert-buffer-function)
;;;;(setq revert-buffer-insert-file-contents-function)
;;;;(setq rlogin-explicit-args)
;;;;(setq rlogin-mode-hook)
;;;;(setq rlogin-mode-map)
;;;;(setq rlogin-password-paranoia)
;;;;(setq rlogin-process-connection-type)
;;;;(setq rlogin-program)
;;;;(setq rmail-default-dont-reply-to-names)
;;;;(setq rmail-delete-after-output)
;;;;(setq rmail-dont-reply-to-names)
;;;;(setq rmail-file-name)
;;;;(setq rmail-ignored-headers)
;;;;(setq rmail-last-file)
;;;;(setq rmail-last-rmail-file)
;;;;(setq rmail-mail-new-frame)
;;;;(setq rmail-primary-inbox-list)
;;;;(setq rmail-show-message-hook)
;;;;(setq rmail-spool-directory)
;;;;(setq run-hooks)
;;;;(setq save-abbrevs)
;;;;(setq scroll-step)
;;;;(setq search-exit-option)
;;;;(setq search-highlight)
;;;;(setq search-nonincremental-instead)
;;;;(setq search-ring)
;;;;(setq search-ring-max)
;;;;(setq search-ring-update)
;;;;(setq search-ring-yank-pointer)
;;;;(setq search-slow-speed)
;;;;(setq search-slow-window-lines)
;;;;(setq search-upper-case)
;;;;(setq search-whitespace-regexp)
;;;;(setq selection-converter-alist)
;;;;(setq selective-display)
;;;;(setq selective-display-ellipses)
;;;;(setq send-mail-function)
;;;;(setq sendmail-program)
(setq sentence-end "[.?!][]\"')]*\\($\\|\t\\| \\)[ \t\n]*")
;;;;(setq server-buffer-clients)
;;;;(setq server-clients)
;;;;(setq server-previous-string)
;;;;(setq server-process)
;;;;(setq server-program)
;;;;(setq server-switch-hook)
;;;;(setq server-temp-file-regexp)
;;;;(setq server-visit-hook)
;;;;(setq server-window)
;;;;(setq shared-lisp-mode-map)
(setq shell-cd-regexp "cd?\\|,+")
;;;;(setq shell-command-history)
;;;;(setq shell-command-regexp)
;;;;(setq shell-completion-execonly)
;;;;(setq shell-delimiter-argument-list)
;;;;(setq shell-directory-stack)
;;;;(setq shell-dirstack)
;;;;(setq shell-dirstack-query)
;;;;(setq shell-file-name)
;;;;(setq shell-last-dir)
(setq shell-mode-hook 'shell-mode-hook)
;;;;(setq shell-mode-map)
(setq shell-popd-regexp "popd\\|>")
(setq shell-prompt-pattern "^.*[]{}#$%>[] ")
(setq comint-prompt-regexp shell-prompt-pattern)
;;;;(setq shell-pushd-dextract)
;;;;(setq shell-pushd-dunique)
(setq shell-pushd-regexp "pushd\\|<")
;;;;(setq shell-pushd-tohome)
;;;;(setq shell-set-directory-error-hook)
;;;;(setq show-paren-face)
;;;;(setq show-paren-mismatch-face)
;;;;(setq show-paren-overlay)
;;;;(setq show-paren-overlay-1)
;;;;(setq site-run-file)
;;;;(setq slitex-run-command)
;;;;(setq snarf-tag-function)
;;;;(setq solar-holidays)
;;;;(setq spell-command)
;;;;(setq spell-filter)
;;;;(setq split-height-threshold)
;;;;(setq split-window-keep-point)
;;;;(setq stack-trace-on-error)
;;;;(setq standard-display-table)
;;;;(setq standard-input)
;;;;(setq standard-output)
;;;;(setq suspend-hook)
;;;;(setq system-type)
;;;;(setq t)
;;;;(setq tab-stop-list)
;;;;(setq tab-width)
;;;;(setq tag-lines-already-matched)
;;;;(setq tags-add-tables)
;;;;(setq tags-apropos-function)
;;;;(setq tags-completion-table)
;;;;(setq tags-completion-table-function)
;;;;(setq tags-file-name)
;;;;(setq tags-included-tables)
;;;;(setq tags-included-tables-function)
;;;;(setq tags-location-stack)
;;;;(setq tags-loop-operate)
;;;;(setq tags-loop-scan)
;;;;(setq tags-table-files)
;;;;(setq tags-table-files-function)
;;;;(setq tags-table-format-hooks)
;;;;(setq tags-table-list)
;;;;(setq tags-table-list-pointer)
;;;;(setq tags-table-list-started-at)
;;;;(setq tags-table-parent-pointer-list)
;;;;(setq tags-table-set-list)
;;;;(setq tc-mode-map)
;;;;(setq temp-buffer-show-function)
;;;;(setq temporary-goal-column)
;;;;(setq term-file-prefix)
;;;;(setq term-setup-hook)
;;;;(setq terminal-frame)
;;;;(setq text-mode-abbrev-table)
;;;;(setq text-mode-map)
;;;;(setq text-mode-syntax-table)
;;;;(setq thing@pt-version)
;;;;(setq this-command)
;;;;(setq time-stamp-active)
;;;;(setq time-stamp-end)
;;;;(setq time-stamp-format)
;;;;(setq time-stamp-line-limit)
;;;;(setq time-stamp-mail-host)
;;;;(setq time-stamp-month-full-names)
;;;;(setq time-stamp-month-numbers)
;;;;(setq time-stamp-start)
;;;;(setq top-level)
;;;;(setq trace-buffer)
;;;;(setq track-eol)
;;;;(setq transient-mark-mode)
;;;;(setq trim-versions-without-asking)
;;;;(setq truncate-lines)
;;;;(setq truncate-partial-width-windows)
;;;;(setq undo-limit)
;;;;(setq undo-strong-limit)
;;;;(setq unread-command-char)
;;;;(setq unread-command-events)
;;;;(setq v)
;;;;(setq val)
;;;;(setq values)
;;;;(setq vc-binary-assoc)
;;;;(setq vc-checkin-hook)
;;;;(setq vc-checkin-switches)
;;;;(setq vc-checkout-carefully)
;;;;(setq vc-checkout-writable-buffer-hook)
;;;;(setq vc-command-messages)
;;;;(setq vc-comment-alist)
;;;;(setq vc-comment-ring)
;;;;(setq vc-comment-ring-index)
;;;;(setq vc-default-back-end)
;;;;(setq vc-dired-mode)
;;;;(setq vc-dired-prefix-map)
;;;;(setq vc-file-prop-obarray)
;;;;(setq vc-header-alist)
;;;;(setq vc-initial-comment)
;;;;(setq vc-keep-workfiles)
;;;;(setq vc-last-comment-match)
;;;;(setq vc-log-after-operation-hook)
;;;;(setq vc-log-entry-mode)
;;;;(setq vc-log-operation)
;;;;(setq vc-make-backup-files)
;;;;(setq vc-master-templates)
;;;;(setq vc-maximum-comment-ring-size)
;;;;(setq vc-mistrust-permissions)
;;;;(setq vc-mode)
;;;;(setq vc-name-assoc-file)
;;;;(setq vc-parent-buffer)
;;;;(setq vc-parent-buffer-name)
;;;;(setq vc-path)
;;;;(setq vc-prefix-map)
;;;;(setq vc-rcs-status)
;;;;(setq vc-static-header-alist)
;;;;(setq vc-suppress-confirm)
;;;;(setq verify-tags-table-function)
(setq version-control t)
(setq visible-bell t)
;;;;(setq vm-preview-message-hook)
;;;;(setq vm-show-message-hook)
;;;;(setq vm-summary-pointer-hook)
;;;;(setq vms-stmlf-recfm)
(setq window-min-height 2)
(setq window-min-width  2)
;;;;(setq window-setup-hook)
;;;;(setq window-system)
;;;;(setq window-system-version)
;;;;(setq words-include-escapes)
;;;;(setq write-contents-hooks)
;;;;(setq write-file-hooks)
;;;;(setq write-region-annotate-functions)
;;;;(setq x-colors)
;;;;(setq x-command-line-resources)
;;;;(setq x-cursor-fore-pixel)
;;;;(setq x-cut-buffer-max)
;;;;(setq x-display-name)
;;;;(setq x-fixed-font-alist)
;;;;(setq x-font-regexp)
;;;;(setq x-font-regexp-head)
;;;;(setq x-font-regexp-slant)
;;;;(setq x-font-regexp-weight)
;;;;(setq x-invocation-args)
;;;;(setq x-last-selected-text)
;;;;(setq x-lost-selection-hooks)
;;;;(setq x-no-window-manager)
;;;;(setq x-resource-name)
;;;;(setq x-selection-timeout)
;;;;(setq x-sent-selection-hooks)
;;;;(setq x-switch-definitions)
;;;;(setq yank-menu-length)
